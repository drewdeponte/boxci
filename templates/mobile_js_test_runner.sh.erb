#!/bin/bash --login

export RAILS_ENV=test
export PROJECT_DIR="/vagrant/<%= @config['project_name'] %>"
export CI_REPORTS="/vagrant/<%= @config['project_name'] %>/reports"

sudo ssh -oStrictHostKeyChecking=no github.com
cd $PROJECT_DIR 
pwd
echo "running setup from ci_bootstrap.yml"
#<%= @config["project_setup"] %>

echo "running tests <%= @config["tests_to_run"] %>"
<%= @config["tests_to_run"] %>

echo "running post_project_run"
<%= @config["post_project_run"] %>
